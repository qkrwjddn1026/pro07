<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="free">
	<select id="freeList" resultType="kr.go.me.dto.FreeDTO">
		select * from free
	</select>
	
	<select id="freeDetail" resultType="kr.go.me.dto.FreeDTO">
		select * from free where bno= #{bno}
	</select>
	
	<select id="freeInsert">
		insert into free values ((select nvl(max(bno), 0)+1 from free), #{title}, #{content}, #{nickname }, sysdate, 0)
	</select>
	
	<select id="freeEdit">
		update free set title = #{title }, content = #{content } where bno = ${bno }
	</select>
	
	<select id="freeDelete" >
		delete from free where bno = #{bno }
	</select>
	
	<select id="visitedCount">
		update free set visited = visited + 1 where bno = #{bno }
	</select>
</mapper>